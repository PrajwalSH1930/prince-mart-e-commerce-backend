server:
  port: 8086

spring:
  application:
    name: PAYMENT-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/pm_payment_db
    username: root
    password: Sprince1*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true # Explicitly tell it to register
    fetch-registry: true       # Explicitly tell it to fetch other services
    service-url:
      defaultZone: http://localhost:8761/eureka/

razorpay:
  key:
    id: rzp_test_SWXiqwcwXnFIvp
    secret: JxvZ3SPu0lnRA9W9WgiEW4ET