server:
  port: 8090

spring:
  application:
    name: wishlist-service
  datasource:
    url: jdbc:mysql://localhost:3306/pm_wishlist_db
    username: root
    password: Sprince1*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/