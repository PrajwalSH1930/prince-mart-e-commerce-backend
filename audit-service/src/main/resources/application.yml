server:
  port: 8092

spring:
  application:
    name: audit-service
  datasource:
    url: jdbc:mysql://localhost:3306/pm_audit_db
    username: root
    password: Sprince1*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/