server:
  port: 8084

spring:
  application:
    name: CART-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/pm_cart_db
    username: root
    password: Sprince1*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/