name: Keep Backend Alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Service
        run: curl -s https://your-service.onrender.com/actuator/health
